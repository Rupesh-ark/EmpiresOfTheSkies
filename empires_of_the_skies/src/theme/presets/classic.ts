import type { TokenPreset } from "./types";

/**
 * CLASSIC PRESET
 * ──────────────
 * The original parchment theme — no overrides.
 * Background range: 87% → 93% lightness (6% spread)
 * Accent: gold only, no secondary accent.
 */
export const classicPreset: TokenPreset = {
  name: "classic",
  description: "Original parchment theme — warm monochrome, gold accent only",
};
