packages:
  - "empires_of_the_skies"
  - "server"
  - "packages/*"

allowBuilds:
  esbuild: true

overrides:
  # Unreleased test build from boardgame.io commit c92e91c.
  # Keep all direct and peer dependency resolutions on the same protocol version.
  boardgame.io: file:vendor/boardgame.io-0.51.0-beta.4.tgz
